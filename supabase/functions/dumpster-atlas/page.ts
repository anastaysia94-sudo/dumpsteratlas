export function renderPage(canonical: string) {
  return `<!doctype html><html lang="en"><head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Dumpster Atlas | Legal Reuse, Food & Recycling Map — San Jose</title>
<meta name="description" content="Find public, legal recycling, CRV, food-assistance and reuse locations around San Jose and Santa Clara County, with an optional live OpenStreetMap recycling layer.">
<meta name="robots" content="index,follow,max-image-preview:large"><link rel="canonical" href="${canonical}">
<meta property="og:type" content="website"><meta property="og:title" content="Dumpster Atlas — Public Reuse & Recycling Map"><meta property="og:description" content="Verified public resources plus a clearly labeled community OpenStreetMap recycling layer."><meta property="og:url" content="${canonical}">
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="">
<script type="application/ld+json">{"@context":"https://schema.org","@type":"WebApplication","name":"Dumpster Atlas","applicationCategory":"NavigationApplication","operatingSystem":"Any modern web browser","description":"A public-resource map for legal reuse, recycling, food assistance and material drop-off locations in the San Jose area.","url":"${canonical}"}</script>
<style>
:root{--bg:#08110d;--panel:#0f1c16;--text:#edf8f1;--muted:#a9c0b2;--lime:#9cf06b;--mint:#4fd8a0;--amber:#ffc45b;--line:#274535}*{box-sizing:border-box}body{margin:0;background:linear-gradient(180deg,#07110c,#0b1711);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}header{padding:24px 20px 18px;border-bottom:1px solid var(--line);background:rgba(8,17,13,.94);position:sticky;top:0;z-index:1000;backdrop-filter:blur(14px)}.head{max-width:1180px;margin:auto;display:flex;justify-content:space-between;gap:18px;align-items:center}.brand{font-weight:950;font-size:1.25rem}.brand span{color:var(--lime)}.safe{font-size:.82rem;color:var(--muted);max-width:520px;text-align:right}.wrap{max-width:1180px;margin:auto;padding:22px 20px 60px}.hero{display:grid;grid-template-columns:1.3fr .7fr;gap:20px;margin-bottom:20px}.intro,.rules{border:1px solid var(--line);border-radius:20px;background:var(--panel);padding:22px}.intro h1{margin:0 0 8px;font-size:clamp(2rem,5vw,3.6rem);line-height:1}.intro p,.rules p{color:var(--muted)}.rules b{color:var(--lime)}.controls{display:flex;gap:10px;flex-wrap:wrap;margin:16px 0}.chip,.select,.locate,.community,.searchbox{background:#112019;color:var(--text);border:1px solid var(--line);padding:10px 12px;border-radius:999px;font-weight:800}.chip,.locate,.community{cursor:pointer}.chip.active{background:var(--lime);color:#09200e;border-color:var(--lime)}.locate{background:linear-gradient(135deg,var(--mint),var(--lime));color:#082014;border:0}.community{border-color:#765d24;color:#ffe2a0;background:#241d0f}.community.active{background:var(--amber);color:#2b2002}.searchbox{min-width:210px;outline:none}.searchbox:focus{border-color:var(--lime)}.legend{display:flex;gap:12px;flex-wrap:wrap;font-size:.8rem;color:var(--muted);margin:8px 0 14px}.legend i{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:5px}.verifiedDot{background:var(--lime)}.communityDot{background:var(--amber)}.status{font-size:.9rem;color:var(--muted);min-height:1.4em}.layout{display:grid;grid-template-columns:1.25fr .75fr;gap:18px}.mapWrap{position:relative}.mapBadge{position:absolute;z-index:500;top:12px;left:12px;background:rgba(7,17,12,.9);border:1px solid var(--line);border-radius:12px;padding:8px 10px;font-size:.8rem}#map{height:610px;border-radius:22px;border:1px solid var(--line);overflow:hidden}.list{display:flex;flex-direction:column;gap:12px;max-height:610px;overflow:auto;padding-right:4px}.card{border:1px solid var(--line);border-radius:17px;background:var(--panel);padding:16px}.tag{display:inline-flex;background:#193123;color:#bdf6d2;border:1px solid #31593f;border-radius:999px;padding:3px 8px;font-size:.74rem;font-weight:800;margin:2px 3px 2px 0}.card h3{margin:5px 0}.card p{margin:6px 0;color:var(--muted);font-size:.92rem}.card .address{color:#d8eadf}.actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:11px}.btn{display:inline-block;padding:9px 11px;border-radius:10px;text-decoration:none;font-weight:850;font-size:.86rem}.directions{background:var(--lime);color:#09200e}.source{border:1px solid var(--line);color:#d7ede0}.verified{font-size:.75rem;color:#86a995;margin-top:8px}.empty{padding:30px;border:1px dashed var(--line);border-radius:16px;color:var(--muted)}footer{margin-top:28px;border-top:1px solid var(--line);padding-top:20px;color:var(--muted);font-size:.86rem}.leaflet-popup-content-wrapper,.leaflet-popup-tip{background:#102016;color:#edf8f1}.leaflet-container a{color:#0a6949}@media(max-width:850px){.hero,.layout{grid-template-columns:1fr}.safe{text-align:left}.head{align-items:flex-start;flex-direction:column}#map{height:440px}.list{max-height:none}.searchbox{min-width:100%}}
</style></head><body>
<header><div class="head"><div class="brand">🗺️ <span>DUMPSTER</span> ATLAS</div><div class="safe">Public resources only. No private dumpsters, locked enclosures, gated bins, or “the sign probably doesn’t apply to me” logic.</div></div></header>
<main class="wrap"><section class="hero"><div class="intro"><h1>Useful stuff, without the trespassing.</h1><p>Find source-checked recycling, CRV, food-assistance and reuse locations around San Jose and Santa Clara County. Optionally add live community recycling points from OpenStreetMap as a separate trust layer.</p></div><div class="rules"><b>Two trust levels</b><p><b>Green</b> = source-checked by Dumpster Atlas. <span style="color:var(--amber);font-weight:800">Amber</span> = community OpenStreetMap data loaded live and not individually verified by us.</p></div></section>
<div class="controls" id="cats"><input id="search" class="searchbox" type="search" placeholder="Search verified resources…" aria-label="Search verified resources"><button class="chip active" data-filter="all">All</button><button class="chip" data-filter="food">Food</button><button class="chip" data-filter="cans">Cans & bottles</button><button class="chip" data-filter="reuse">Clothing & reuse</button><button class="chip" data-filter="electronics">Electronics</button><button class="chip" data-filter="useful-materials">Useful materials</button><select id="radius" class="select" aria-label="Search radius"><option value="2">2 miles</option><option value="5">5 miles</option><option value="10" selected>10 miles</option><option value="15">15 miles</option><option value="20">20 miles</option></select><button class="locate" id="locate">◎ Use my location</button><button class="community" id="osmBtn">＋ Community OSM layer</button></div>
<div class="legend"><span><i class="verifiedDot"></i>Verified curated resources</span><span><i class="communityDot"></i>Live community OpenStreetMap recycling points</span></div>
<div class="status" id="status">Showing source-checked public resources. Radius filtering starts after you choose “Use my location.”</div>
<div class="layout"><div class="mapWrap"><div class="mapBadge" id="count">Loading…</div><div id="map"></div></div><div class="list" id="list"></div></div>
<footer><b>Privacy:</b> precise browser location is used only on your device for distance calculations. Analytics records interaction types, not coordinates or search text. The optional OSM layer sends the visible map box, not your GPS point, to our server and then to OpenStreetMap’s Overpass service.</footer></main>
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin=""></script>
<script>
const endpoint=location.href.split('?')[0];let sid=localStorage.getItem('da_sid');if(!sid){sid=crypto.randomUUID();localStorage.setItem('da_sid',sid)}
function track(event_name,meta={},resource_id=null){fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({event_name,session_id:sid,meta,resource_id}),keepalive:true}).catch(()=>{})}
track('page_view',{ref:document.referrer?new URL(document.referrer).hostname:'direct'});
const map=L.map('map').setView([37.3382,-121.8863],11);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'&copy; OpenStreetMap contributors'}).addTo(map);const verifiedLayer=L.layerGroup().addTo(map);const communityLayer=L.layerGroup().addTo(map);let resources=[];let current='all';let userPos=null;let radius=10;let searchTerm='';let osmLoaded=false;let userMarker=null;
function miles(a,b,c,d){const R=3958.8,p=x=>x*Math.PI/180;const dLat=p(c-a),dLon=p(d-b);const q=Math.sin(dLat/2)**2+Math.cos(p(a))*Math.cos(p(c))*Math.sin(dLon/2)**2;return 2*R*Math.asin(Math.sqrt(q))}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function matches(r){const t=r.tags||[];const hay=(r.name+' '+r.address+' '+r.description+' '+t.join(' ')).toLowerCase();if(searchTerm&&!hay.includes(searchTerm))return false;if(current==='all')return true;if(current==='food')return r.category==='food'||t.includes('food');if(current==='cans')return t.some(x=>['cans','bottles','crv'].includes(x));if(current==='reuse')return r.category==='reuse'||t.some(x=>['clothing','reuse'].includes(x));if(current==='electronics')return t.includes('electronics');if(current==='useful-materials')return t.some(x=>['useful-materials','building-materials','furniture','appliances','metal'].includes(x));return true}
function within(r){return !userPos||miles(userPos.lat,userPos.lng,r.latitude,r.longitude)<=radius}
function render(){verifiedLayer.clearLayers();const filtered=resources.filter(r=>matches(r)&&within(r));document.getElementById('count').textContent=filtered.length+' verified'+(osmLoaded?' + community':'');const list=document.getElementById('list');list.innerHTML='';if(!filtered.length)list.innerHTML='<div class="empty">No verified resource matches this filter yet. Try another category, widen the radius, or load the community OSM layer.</div>';filtered.forEach(r=>{const marker=L.circleMarker([r.latitude,r.longitude],{radius:8,color:'#9cf06b',fillColor:'#9cf06b',fillOpacity:.85}).addTo(verifiedLayer);marker.bindPopup('<b>'+esc(r.name)+'</b><br>'+esc(r.address)+'<br><small>Verified curated record</small>');marker.on('click',()=>track('resource_open',{surface:'map'},r.id));const distance=userPos?miles(userPos.lat,userPos.lng,r.latitude,r.longitude).toFixed(1)+' mi · ':'';const tags=(r.tags||[]).slice(0,5).map(t=>'<span class="tag">'+esc(t.replaceAll('-',' '))+'</span>').join('');const div=document.createElement('article');div.className='card';div.innerHTML='<div>'+tags+'</div><h3>'+esc(r.name)+'</h3><p class="address">'+distance+esc(r.address)+'</p><p>'+esc(r.description)+'</p><p>'+esc(r.access_note)+'</p><div class="actions"><a class="btn directions" target="_blank" rel="noopener" href="https://www.google.com/maps/dir/?api=1&destination='+r.latitude+','+r.longitude+'">Directions</a><a class="btn source" target="_blank" rel="noopener" href="'+esc(r.source_url)+'">Check source</a></div><div class="verified">Source checked '+esc(r.verified_at)+'</div>';div.querySelector('.directions').addEventListener('click',()=>track('directions_click',{},r.id));div.querySelector('.source').addEventListener('click',()=>track('source_click',{},r.id));div.addEventListener('click',e=>{if(!e.target.closest('a'))track('resource_open',{surface:'list'},r.id)});list.appendChild(div)});if(filtered.length>1&&!userPos){const b=L.latLngBounds(filtered.map(r=>[r.latitude,r.longitude]));map.fitBounds(b.pad(.15),{maxZoom:13})}}
async function loadOsm(){const b=map.getBounds();document.getElementById('status').textContent='Loading live community recycling points from OpenStreetMap…';const q='?api=osm&s='+b.getSouth().toFixed(5)+'&w='+b.getWest().toFixed(5)+'&n='+b.getNorth().toFixed(5)+'&e='+b.getEast().toFixed(5);try{const r=await fetch(endpoint+q);const j=await r.json();if(!r.ok)throw new Error(j.error||'load_failed');communityLayer.clearLayers();const pts=j.points||[];pts.forEach(p=>{const m=L.circleMarker([p.latitude,p.longitude],{radius:6,color:'#ffc45b',fillColor:'#ffc45b',fillOpacity:.78}).addTo(communityLayer);const mats=(p.materials||[]).slice(0,6).join(', ');m.bindPopup('<b>'+esc(p.name)+'</b><br><span style="color:#ffc45b">Community OSM data</span><br>'+(mats?'Materials: '+esc(mats)+'<br>':'')+(p.opening_hours?'Hours: '+esc(p.opening_hours)+'<br>':'')+'<a target="_blank" rel="noopener" href="'+esc(p.source_url)+'">OpenStreetMap record</a>');m.on('click',()=>track('osm_open',{materials:(p.materials||[]).length},p.id))});osmLoaded=true;document.getElementById('osmBtn').classList.add('active');document.getElementById('osmBtn').textContent='↻ Refresh community layer';document.getElementById('status').textContent='Loaded '+pts.length+' community recycling points. Amber pins are live OSM data and are not individually source-checked by Dumpster Atlas.';track('osm_load',{count:pts.length});render()}catch{document.getElementById('status').textContent='Community map data is temporarily unavailable. Verified resources still work.'}}
fetch(endpoint+'?api=resources').then(r=>r.json()).then(j=>{resources=j.resources||[];render()}).catch(()=>{document.getElementById('status').textContent='Could not load verified resources. Try again in a moment.'});
document.querySelectorAll('.chip').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.chip').forEach(x=>x.classList.remove('active'));b.classList.add('active');current=b.dataset.filter;track('category_filter',{filter:current});render()}));
document.getElementById('radius').addEventListener('change',e=>{radius=Number(e.target.value);track('radius_change',{miles:radius});render()});let searchTimer;document.getElementById('search').addEventListener('input',e=>{clearTimeout(searchTimer);searchTerm=e.target.value.trim().toLowerCase();searchTimer=setTimeout(()=>{const resultCount=resources.filter(r=>matches(r)&&within(r)).length;track('search',{query_length:searchTerm.length,results:resultCount});render()},250)});document.getElementById('osmBtn').addEventListener('click',loadOsm);
document.getElementById('locate').addEventListener('click',()=>{if(!navigator.geolocation){document.getElementById('status').textContent='This browser does not expose location.';return}document.getElementById('status').textContent='Getting your location…';navigator.geolocation.getCurrentPosition(pos=>{userPos={lat:pos.coords.latitude,lng:pos.coords.longitude};track('use_location',{radius});if(userMarker)map.removeLayer(userMarker);userMarker=L.circleMarker([userPos.lat,userPos.lng],{radius:8,color:'#4fd8a0',fillColor:'#4fd8a0',fillOpacity:.8}).addTo(map).bindPopup('You are here (browser-only distance calculation)');map.setView([userPos.lat,userPos.lng],Math.max(map.getZoom(),12));document.getElementById('status').textContent='Using your location for distance only. Coordinates were not sent to Dumpster Atlas analytics.';render()},()=>{track('location_denied');document.getElementById('status').textContent='Location permission was not granted. Showing the full map instead.'},{enableHighAccuracy:false,timeout:8000,maximumAge:300000})});
</script></body></html>`;
}
